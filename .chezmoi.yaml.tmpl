hooks:
  apply:
    pre:
      args:
        - -c
        - cd $CHEZMOI_SOURCE_DIR; cue export --force --outfile .chezmoidata.yaml .chezmoidata.cue; cue export --force --outfile .chezmoiexternal.yaml .chezmoiexternal.cue
      command: sh
interpreters:
  ps1:
    args:
      - '-NoLogo'
    command: pwsh
