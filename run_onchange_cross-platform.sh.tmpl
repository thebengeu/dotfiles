#!/usr/bin/env sh
cargo install cargo-update
cargo install hyperfine
cargo install lsd
cargo install sd
cargo install tere
cargo install tokei
cargo install topgrade

go install github.com/nao1215/gup@latest

pip install --user pipx

pipx install beancount
pipx install fava
pipx install khal
pipx install neovim-remote
pipx install qmk
pipx install sqlfluff
pipx install tox
pipx install virtualenv
pipx install visidata

pnpm add --global npm-check-updates
pnpm add --global pino-pretty
pnpm add --global pm2

CHT_SH_PATH=~/.local/bin/cht.sh
curl -s https://cht.sh/:cht.sh >$CHT_SH_PATH

{{ if or (ne .chezmoi.os "windows") (ne .chezmoi.arch "arm64") }}
cargo install tealdeer
cargo install xh

pipx install howdoi

pnpm add --global https://github.com/thebengeu/ts-node.git
{{ end }}

{{ if eq .chezmoi.os "windows" }}
cargo install bat
cargo install fd-find
{{ else }}
cargo install gitui

chmod +x $CHT_SH_PATH
{{ end }}
