#!/usr/bin/env sh
aws-vault exec --no-session bedrock -- \
	docker run \
	--detach \
	--env AWS_REGION=us-east-1 \
	--env AWS_ACCESS_KEY_ID \
	--env AWS_SECRET_ACCESS_KEY \
	--name bedrock-gateway \
	--publish 2337:80 \
	--restart unless-stopped \
	ibehren1/bedrock-gateway
